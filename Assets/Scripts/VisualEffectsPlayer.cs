using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using static MovementManager;

public class VisualEffectsPlayer : MonoBehaviour
{


    // private bool particlesLaunched = true;

    // // Update is called once per frame
    // void Update()
    // {
    //     if (derape && !particlesLaunched && listRoues.Count == 4) {
    //         for (int i = 0; i < 4;){
    //             Instantiate(derapageParticles, listRoues[i]);
    //         }
    //         particlesLaunched = true;
    //     }
    // }
}
